# Windows Capability Matrix

This matrix records current evidence, not intended behavior. A unit, fixture, or mock result
is not evidence of a live Palworld server or a real player session.

| Capability | Linux state | Windows state | Automated evidence | Live evidence | Current gap |
| --- | --- | --- | --- | --- | --- |
| Runtime layout and managed paths | Existing package-local data layout | Repository development layout defaults to `dev-runtime\\windows`; explicit runtime override is supported | `internal/appconfig` tests, `scripts/test-windows-paths.ps1`, package verification, and Launcher smoke | Path contract and packaged Launcher smoke passed from `D:\\WL\\me\\pal\\dev-runtime\\windows` | Release candidate is still unsigned and needs the intended Windows-version QA matrix |
| SteamCMD and server install | Docker/Wine path retained | Native SteamCMD download, safe extraction, retry, cancellation, staging, and install validation | `internal/server`, `internal/steamcmd`, and `internal/mods` tests | SteamCMD installed Palworld Dedicated Server Build ID `24181105`; manifest `StateFlags=4`, download byte counts match, and the installation is about 5.75 GiB | Repeat the check for the final release candidate on a clean host |
| Palworld process lifecycle | Docker lifecycle retained | PID, executable path and creation time are rechecked; the owned Windows process tree is stopped through a Job Object. Current `PalServer-Win64-Shipping-Cmd.exe` is preferred, with legacy `PalServer-Win64-Test-Cmd.exe` fallback | `internal/server` lifecycle, identity, and tree-stop tests | `e2e-20260715T074106247Z-68236-d74024f8` started and restarted the current Shipping-Cmd process, observed owned UDP `8211`/`27015` endpoints and a lifecycle log, then left no tracked server process | No real-player session is implied by the lifecycle result |
| Workshop, HTTPS, GitHub, and local ZIP Mod import | Existing supported sources retained | Workshop alone is gated by an account-name-only local SteamCMD login, config ACL hardening, cached-session verification, API permissions, and a true loopback check. Password and Steam Guard input stay in the SteamCMD console. GitHub/HTTPS/local ZIP remain outside this gate | Import, native Workshop, Steam login/cache, ACL target, API permission, loopback, and frontend gate tests | The cached login was reused by a new SteamCMD process and Workshop item `3625364851` downloaded successfully (279,418 bytes) | Complete the same login and download through the packaged desktop UI and retain UI evidence |
| Local Mod scan | N/A | Scan and reconciliation recognize Workshop, Pak, UE4SS, links, and Windows case rules; import, ignore, repair, confirmation, and delete actions are implemented | `internal/mods` scan/action tests plus API and Mods page tests | Fixture filesystem only | Retain one real manually installed Mod scan/action run |
| OpenAI-compatible translation | Existing provider support | Explicit proxy, custom headers, timeout, cancellation classes, and secret-safe persisted configuration | `internal/aitranslation`, API, Settings API/page tests | Local mock provider only | Real API is intentionally opt-in and has not been called |
| UE4SS dependency | N/A | PalDefender installation always checks/installs pinned UE4SS `v3.0.1` first, with hash validation, rollback, preservation, and compatibility state | `internal/paldefender` UE4SS tests | The live run recorded `ue4ss_log` evidence from native `UE4SS.log`, including UE4SS startup and object-array discovery | Repeat against the final release candidate |
| PalDefender | Existing integration retained | PalDefender is outside the Workshop login gate; installation preserves UE4SS ordering and uses transactional install/rollback plus native-log status mapping | `internal/paldefender` and Security page tests | PalDefender `1.8.1+bundled` installed idempotently and the live run recorded `paldefender_log` evidence containing `Starting PalDefender Anti Cheat v1.8.1` | Live GM health/actions still require a disposable player |
| GM tools | Existing API/UI retained | Permission gates, strict protocol mapping, confirmations, audit records, and idempotency controls are implemented | Backend loopback mock and frontend protocol/UI suites | No explicitly marked test player | Report as `mock/protocol verified; live player pending` until the documented manual test is completed |
| Launcher | Existing package launcher retained | Runtime root, child Job Object, health checks, flags, and log handling | Launcher tests, package verification, and smoke scripts | Native package build, PE/content validation, and packaged Launcher smoke passed | Authenticode signing and wider Windows-version QA remain |
| Upgrade, uninstall, recovery | Existing backup/update support | Transactional upgrade, bounded uninstall, configuration recovery, rollback, quoted-path handling, and process cleanup are implemented | Windows path and maintenance contract suites | Packaged maintenance tests passed, including rollback and repeated recovery selection | Perform an operator rehearsal between actual release-version ZIPs before publication |

## Remaining Live Validation

The retained evidence above proves the native SteamCMD/game install, Shipping-Cmd lifecycle,
authenticated SteamCMD Workshop download, package smoke, and native UE4SS/PalDefender load
logs. Before declaring the Windows release complete, follow `docs/windows-validation.md` to
repeat those checks against the final candidate, complete the packaged UI Workshop flow,
scan one real manually installed Mod, and perform GM actions only with an explicitly
disposable test player. If an external platform prevents a live check, retain a redacted
reproducible error and command instead of claiming success.
