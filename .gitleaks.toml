title = "PalPanel current-tree secret scanning"

[extend]
useDefault = true

[[allowlists]]
description = "Generated dependency locks and explicit test fixtures"
paths = [
  '''(^|/)frontend/package-lock\.json$''',
  '''(^|/)(backend|sav-cli)/go\.sum$''',
  '''(^|/).*_test\.go$''',
  '''(^|/).*\.test\.(ts|tsx)$''',
  # Generated public localization data contains item IDs such as
  # TreasureBoxKey01 that resemble generic credential assignments.
  '''(^|/)backend/internal/pallocalize/catalog\.zh-CN\.json$''',
  '''(^|/)data/''',
  '''(^|/)node_modules/''',
  '''(^|/)dist/''',
]
regexes = [
  '''replace-with-a-random-32-byte-token''',
  '''process-environment-token-value''',
]
